{
  "name": "indiana",
  "version": "0.0.0",
  "description": "Experimental isomorphic JavaScript demo",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify client/index.js > public/build/index.js && echo DONE!",
    "serve": "npm run build && node server"
  },
  "author": "Mark Dalgleish",
  "license": "MIT",
  "dependencies": {
    "express": "^4.6.1",
    "react": "^0.10.0",
    "react-router-component": "^0.19.0"
  },
  "devDependencies": {
    "browserify": "^4.2.3"
  }
}
